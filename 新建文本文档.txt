var i of m 遍历 es6遍历对象

...+数组 就会把数组拆解
concat 数组合并
...把数组拆解为单个元素 也是数组扩展

...y 动态的 
...+参数 就是动态参数 。。。+数组就会把数组拆解

promise es6 解决callback （resolve，regect）
成功 失败
return promise

.catch reject 是失败 不是错误
promiss 可以用.then（）的方法进行回调
promise 有一大堆api promise 解决callback回调问题
promise.all（调用多个接口跟请求）

export 导出
看是否暴露
import { 大括号接收} from
import * as util from 这样就把东西封装在*中在用util调用
default 不是默认对象 
暴露的 需要用{}整成对象
import 可以进行异步加载 后加载进去

2018 -3-27 









